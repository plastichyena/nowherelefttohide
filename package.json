{
  "name": "nowhere-left-to-hide",
  "version": "1.5.1",
  "private": true,
  "description": "Nowhere Left to Hide - a mobile-first hex strategy game PoC",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:random": "vite-node --script src/agent/sim-cli.ts --agent=random --summary-only --out=output/simulations/random-smoke --overwrite",
    "test:balanced": "vite-node --script src/agent/sim-cli.ts --agent=balanced --summary-only --out=output/simulations/balanced-smoke --overwrite",
    "test:browser-bridge": "vite-node --script src/testing/browser-bridge-smoke.ts",
    "test:portable": "npm run test:browser-bridge && npm run test:external-ai-e2e",
    "test:external-ai-e2e": "vite-node --script src/testing/external-ai-e2e.ts",
    "session": "vite-node --script src/session/session-cli.ts",
    "sim": "vite-node --script src/agent/sim-cli.ts",
    "typecheck": "tsc --noEmit",
    "build:portable-session": "node scripts/build-portable.mjs"
  },
  "dependencies": {
    "fflate": "^0.8.3",
    "phaser": "^4.2.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "esbuild": "^0.25.12",
    "typescript": "^5.7.3",
    "vite": "^6.1.0",
    "vitest": "^3.0.5"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
